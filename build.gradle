plugins {
    id 'java'
    id 'jacoco'
    id 'maven-publish'
    id 'org.owasp.dependencycheck' version '9.0.9'
    id 'org.asciidoctor.jvm.convert' version '4.0.2'
    id 'java-gradle-plugin'
    id 'com.gradle.plugin-publish' version '1.2.1'
    id 'signing'
}

group = 'io.github.search5.hg4j'
version = '1.0.0'

repositories {
    mavenCentral()
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
    withSourcesJar()
    withJavadocJar()
}

dependencies {
    implementation 'org.apache.commons:commons-compress:1.26.1'
    implementation 'com.github.luben:zstd-jni:1.5.6-3'
    implementation 'com.github.mwiede:jsch:0.2.17'
    implementation 'org.bouncycastle:bcpg-jdk18on:1.78'
    implementation 'org.bouncycastle:bcprov-jdk18on:1.78'
    implementation 'org.bouncycastle:bcutil-jdk18on:1.78'

    testImplementation platform('org.junit:junit-bom:5.10.2')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    
    testImplementation 'org.bouncycastle:bcpg-jdk18on:1.78'
    testImplementation 'org.bouncycastle:bcprov-jdk18on:1.78'
    testImplementation 'org.bouncycastle:bcutil-jdk18on:1.78'
    
    testImplementation 'org.apache.sshd:sshd-core:2.12.0'
    testImplementation 'org.apache.sshd:sshd-common:2.12.0'
    testImplementation 'org.slf4j:slf4j-simple:2.0.12'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
            pom {
                name = 'hg4j'
                description = 'Pure Java implementation of Mercurial (hg) version control system'
                url = 'https://github.com/hg4j/hg4j'
                licenses {
                    license {
                        name = 'The Apache License, Version 2.0'
                        url = 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                    }
                }
                developers {
                    developer {
                        id = 'hg4j'
                        name = 'hg4j contributors'
                    }
                }
                scm {
                    connection = 'scm:git:git://github.com/hg4j/hg4j.git'
                    developerConnection = 'scm:git:ssh://github.com/hg4j/hg4j.git'
                    url = 'https://github.com/hg4j/hg4j'
                }
            }
        }
    }
}

jar {
    from("${rootDir}") {
        include 'LICENSE'
        into 'META-INF'
    }
}

test {
    useJUnitPlatform()
    maxHeapSize = "2048m"
    maxParallelForks = 1
    forkEvery = 100
    // Force complete inclusion of runtime configurations to prevent JPMS/testRuntime resolution drops
    classpath = sourceSets.test.runtimeClasspath + project.configurations.testRuntimeClasspath
    finalizedBy jacocoTestReport
}

jacoco {
    toolVersion = "0.8.11"
}

jacocoTestReport {
    dependsOn test
    reports {
        xml.required = true
        html.required = true
    }
}

jacocoTestCoverageVerification {
    violationRules {
        // 규칙 1: 전체 번들 커버리지 (최소 70% - 네트워크 의존 클래스 제외 전 기준)
        rule {
            element = 'BUNDLE'
            limit {
                minimum = 0.70
            }
        }
        // 규칙 2: 클래스 별 90% 규칙 (외부 의존성이 없는 핵심 클래스만 적용)
        rule {
            element = 'CLASS'
            includes = [
                'com.github.search5.hg4j.dirstate.Dirstate',
                'com.github.search5.hg4j.util.NodeIdUtil',
                'com.github.search5.hg4j.util.SafeFileIO',
                'com.github.search5.hg4j.merge.Merge3',
                'com.github.search5.hg4j.bundle.Bundle2Parser',
                'com.github.search5.hg4j.bundle.ChangegroupParser',
                'com.github.search5.hg4j.api.AddCommand',
                'com.github.search5.hg4j.api.BookmarkCommand',
                'com.github.search5.hg4j.api.BranchCommand',
                'com.github.search5.hg4j.api.InitCommand',
                'com.github.search5.hg4j.api.LogCommand',
                'com.github.search5.hg4j.api.RemoveCommand',
                'com.github.search5.hg4j.api.RevertCommand',
                'com.github.search5.hg4j.api.ShelveCommand',
                'com.github.search5.hg4j.api.StatusCommand',
                'com.github.search5.hg4j.api.TagCommand',
                'com.github.search5.hg4j.api.PushCommand',
                'com.github.search5.hg4j.api.CatCommand',
                'com.github.search5.hg4j.api.DiffCommand',
                'com.github.search5.hg4j.api.TreeCommand',
                'com.github.search5.hg4j.api.UpdateCommand',
            ]
            limit {
                minimum = 0.90
            }
        }
    }
}

check.dependsOn jacocoTestCoverageVerification

dependencyCheck {
    autoUpdate = true
    failBuildOnCVSS = 7.0f
    format = 'ALL'
}

compileJava {
    exclude 'module-info.java'
}

// 2번: Javadoc 다국어(영문/국문) 빌드 설정
javadoc {
    failOnError = false
    options {
        encoding = 'UTF-8'
        docEncoding = 'UTF-8'
        charSet = 'UTF-8'
        locale = 'en_US' // 기본 영어 로케일 설정
        addStringOption('Xdoclint:none', '-quiet')
    }
}

task javadocKo(type: Javadoc) {
    source = sourceSets.main.allJava
    classpath = sourceSets.main.compileClasspath
    destinationDir = file("${buildDir}/docs/javadoc-ko")
    failOnError = false
    options {
        encoding = 'UTF-8'
        docEncoding = 'UTF-8'
        charSet = 'UTF-8'
        locale = 'ko_KR' // 한국어 로케일 설정
        addStringOption('Xdoclint:none', '-quiet')
    }
}

// 1번: Asciidoctor 다국어 문서화 태스크 설정
asciidoctor {
    sourceDir  file('src/docs/asciidoc')
    sources {
        include '**/index.adoc'
    }
    outputDir file('build/docs/asciidoc')
    options doctype: 'book'
    attributes 'source-highlighter': 'coderay',
               'toc': 'left',
               'icons': 'font',
               'sectnums': ''
}

// Gradle Plugin Portal 배포를 위한 플러그인 정의 설정
gradlePlugin {
    website = 'https://github.com/hg4j/hg4j'
    vcsUrl = 'https://github.com/hg4j/hg4j.git'
    plugins {
        hg4jPlugin {
            id = 'io.github.search5.hg4j'
            displayName = 'hg4j Gradle SCM Plugin'
            description = 'Pure Java Mercurial (hg) Gradle integration plugin modeled after JGit.'
            tags.set(['mercurial', 'hg', 'vcs', 'hg4j'])
            implementationClass = 'com.github.search5.hg4j.HgPlugin'
        }
    }
}

// 3번: GPG 아티팩트 서명 설정 (Maven Central 배포 필수 요건)
signing {
    // 실제 저장소에 배포하는 태스크가 수행될 때만 서명을 강력히 의무화하여 로컬 단순 빌드 시 깨지는 현상 방지
    required { gradle.taskGraph.hasTask("publish") || gradle.taskGraph.hasTask("publishToMavenLocal") }
    sign publishing.publications.mavenJava
}

// Gradle 9 암시적 태스크 종속성 경고 및 충돌 방지 설정
tasks.withType(PublishToMavenRepository).configureEach {
    mustRunAfter(tasks.withType(Sign))
}
tasks.withType(PublishToMavenLocal).configureEach {
    mustRunAfter(tasks.withType(Sign))
}

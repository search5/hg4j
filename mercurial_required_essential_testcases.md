# 🔍 hg4j Mercurial 30대 필수 테스트케이스 전수 대명세서

본 명세서는 `hg4j` 라이브러리가 Mercurial (native hg)의 공식 사양을 100% 완벽하게 지원하고, 엔터프라이즈급 초대형 모노레포와 대용량 분산 VCS 서버급 안정성을 보증하기 위해 **머큐리얼 고유 특성에 맞춤 설계된 30대 필수 테스트 케이스**의 상세 명세서입니다.

---

## 📂 카테고리 1: Revlog 및 데이터 스토리지 코어 영역 (Revlog & Storage Core)

### 🗺️ 1. Revlog Inline에서 Non-Inline(64KB 초과)으로의 자동 임계점 기입 전환 테스트
* **💡 네이티브 특성**: Mercurial은 리비전 갯수가 적고 용량이 작을 때 인덱스(`.i`)와 데이터를 한 파일에 합쳐 쓰는 `Inline` 방식을 취하다가, 데이터 크기가 64KB 임계점을 초과하면 인덱스와 데이터(`.d`)를 별도 파일로 자동 분할 저장합니다.
* **🧪 시나리오**: 단일 파일로그에 대해 누적 크기가 65KB인 데이터 쓰기를 수행하여 임계치를 초과하도록 유도합니다.
* **단언 및 검증 기준**: 임계점 돌파 시 물리 파일이 `.i`와 `.d`로 원자적으로 분리 생성되는지 검증합니다.

### 🗺️ 2. Generaldelta 포맷 하에서 최적 압축 체인 검증 테스트
* **💡 네이티브 특성**: `Generaldelta` 형식(0x0002)은 일반 델타와 달리 단순 직전 리비전(`rev - 1`)뿐만 아니라, 임의의 더 유사한 최적의 `baseRev`를 기준(parent 1 or parent 2)으로 삼아 델타를 생성하여 압축률을 극대화합니다.
* **🧪 시나리오**: 여러 브랜치에서 파생된 커밋 그래프를 구축하고 `generaldelta` 모드로 압축을 시뮬레이션합니다.
* **단언 및 검증 기준**: `baseRev`가 순차 부모가 아닌 가장 유사한 이전 base를 가리키며 복원 체인이 손상 없이 유지되는지 단언합니다.

### 🗺️ 3. Zlib ↔ Zstd 다중 압축 알고리즘 상호 운용성 및 디스크 데이터 정합성 테스트
* **💡 네이티브 특성**: Mercurial 리포지토리는 포맷 버전에 따라 `zlib` 또는 최신 `zstd` 형식으로 리비전 데이터를 압축합니다.
* **🧪 시나리오**: 동일한 원문 데이터에 대해 각각 `zlib` 및 `zstd` 엔진을 사용하여 기입하고 읽어옵니다.
* **단언 및 검증 기준**: 상호 간에 압축 해제된 데이터 바이트가 원래 원문과 한 바이트 오차도 없이 100% 동일(Byte Parity)함을 보증합니다.

### 🗺️ 4. Censored 리비전(메타데이터 보존 후 파일 텍스트만 보안 삭제) 접근 제한 테스트
* **💡 네이티브 특성**: 보안상의 이유 등으로 특정 리비전의 파일 내용을 완전히 삭제해야 할 경우, 머큐리얼은 해당 리비전의 메타데이터와 해시는 보존하되 파일 텍스트는 `Censored` 처리하여 읽기 시 예외를 발생시키거나 경고를 줍니다.
* **🧪 시나리오**: 특정 리비전을 Censored 플래그 처리한 후 읽기를 격발합니다.
* **단언 및 검증 기준**: 복원 엔진이 데이터가 아닌 보안 검열 예외를 정상적으로 뿜어내며 읽기가 원천 차단되는지 검증합니다.

### 🗺️ 5. Revlog 파일 손상 시 비정상 바이트 오프셋 감지 및 파싱 Crash 방어 테스트
* **💡 네이티브 특성**: 인덱스 파일(`.i`)이나 데이터 파일(`.d`)이 하드웨어 손상 등으로 깨진 경우, 파서가 비정상적인 오프셋을 가리켜 JVM 크래시가 발생하는 것을 막아야 합니다.
* **🧪 시나리오**: 인덱스 파일의 오프셋 필드에 임의의 훼손된 물리 오프셋 값을 주입하고 파싱을 유도합니다.
* **단언 및 검증 기준**: 파서가 배열 범위 초과 예외 대신 정밀하게 설계된 `HgException` 혹은 `IOException` 계열 예외를 안전하게 송출하는지 검증합니다.

---

## 📂 카테고리 2: Dirstate 및 로컬 워킹 디렉토리 영역 (Dirstate & Working Directory)

### 🗺️ 6. Dirstate V1 ↔ V2 포맷 마이그레이션(Migration) 정합성 테스트
* **💡 네이티브 특성**: 리포지토리 구성 변동에 따라 flat 구조의 `dirstate-v1` 파일에서 최적화된 트리 구조의 `dirstate-v2` (Docket 파일 + 분할 데이터 파일)로 포맷을 마이그레이션할 수 있습니다.
* **🧪 시나리오**: 약 1,000개의 파일 경로 상태를 가진 Dirstate V1 파일을 디스크에 생성한 후 V2 Serializer를 구동해Docket 및 데이터 분할 구조로 변환했다가 다시 역으로 V1 복원을 수행합니다.
* **단언 및 검증 기준**: 양방향 변환 전후의 모든 경로, 파일 상태, 수정 시각 데이터가 단 1바이트의 오차도 없이 일치하는지 단언합니다.

### 🗺️ 7. Dirstate V2 복사/이동(Copy/Rename) 메타데이터 `copyMap` 직렬화 정합성 테스트
* **💡 네이티브 특성**: `dirstate-v2` 이진 구조체는 복사/이동 이력을 기록하는 복사 원본명 및 오프셋 필드(`copyMap`)를 명세에 따라 정상 직렬화 보존해야 합니다.
* **🧪 시나리오**: 특정 파일이 복사/이동된 이력을 설정하고 Dirstate V2 이진 세그먼트로 직렬화 후 다시 역직렬화로 로드합니다.
* **단언 및 검증 기준**: 복사 원본 파일명 및 매핑 정보가 완전 무결하게 판독되는지 단언합니다.

### 🗺️ 8. Dirstate V2 나노초 정밀 타임스탬프(`mtimeNanoseconds`) 변화 감지 및 상태 갱신 식별 테스트
* **💡 네이티브 특성**: Dirstate V2는 4바이트의 나노초 타임스탬프 영역을 사용하여 밀리초 이하의 찰나에 발생한 파일 미세 수정 변동 사항을 정확히 추적합니다.
* **🧪 시나리오**: 동일 밀리초 내에서 나노초 단위를 다르게 변경한 다수의 임시 파일을 생성하여 상태 감지를 테스트합니다.
* **단언 및 검증 기준**: 찰나의 수정 사항도 놓치지 않고 `modified` 상태로 정확하게 포착해내는지 단언합니다.

### 🗺️ 9. Dirstate V2 Docket 손상 시 Dirstate V1 Fallback 안정성 테스트
* **💡 네이티브 특성**: Dirstate V2의 마스터 메타데이터 파일인 Docket이 손상되거나 매직 훼손이 일어날 경우, 자가 진단 및 하위 V1 규격으로 안전하게 폴백 작동해야 합니다.
* **🧪 시나리오**: Docket 파일의 바이너리 헤더를 손상시킨 뒤 Dirstate 복구 및 조회 동작을 수행합니다.
* **단언 및 검증 기준**: 예외로 멈추지 않고, V1으로 안전하게 폴백하거나 무결성 에러를 부드럽게 핸들링하는지 단언합니다.

### 🗺️ 10. 파일 시스템 대소문자 감도(Case-Sensitivity)에 따른 파일 경로 매핑 및 충돌 검증 테스트
* **💡 네이티브 특성**: Windows/macOS(대소문자 미구분, Case-insensitive)와 Linux(대소문자 구분, Case-sensitive) 간의 리포지토리 호환성을 위해 파일명 대소문자 변환 처리를 정밀 조율해야 합니다.
* **🧪 시나리오**: `File.txt`와 `file.txt`를 동시 보존하는 리포지토리를 Case-insensitive 파일 시스템에서 조작할 때 발생하는 오동작을 시뮬레이션합니다.
* **단언 및 검증 기준**: 파일 시스템 특성에 따른 적절한 이스케이프 및 충돌 방어 로직이 정상 동작하는지 단언합니다.

### 🗺️ 11. `.hgignore` 정규식(`regexp:`) 및 글로브(`glob:`) 다중 패턴 매칭 필터링 테스트
* **💡 네이티브 특성**: `.hgignore` 내의 정규식 모드(`syntax: regexp`)와 글로브 모드(`syntax: glob`)가 번갈아 가며 한 파일 내에 복합 선언되어 있어도 충돌 없이 동작해야 합니다.
* **🧪 시나리오**: 복합적인 무작위 ignore 패턴 명단을 설정하고, 수천 개의 파일 경로를 걸러냅니다.
* **단언 및 검증 기준**: native hg의 무시(ignore) 규칙과 한 글자도 틀림없이 동일하게 매칭 대상을 선별하는지 단언합니다.

---

## 📂 카테고리 3: fncache 및 파일 경로 인코딩 영역 (fncache & Encoding)

### 🗺️ 12. Windows 예약 파일명(con, aux, nul, prn 등) 디스크 세이프 이스케이프 처리 검증 테스트
* **💡 네이티브 특성**: Windows 환경의 특수 예약어 파일명들이 스토어 저장 시 파일 생성 충돌을 내는 것을 막기 위해 `con.d`는 `co~6e.d` 등 특정 이스케이프 문자로 치환 및 인코딩됩니다.
* **🧪 시나리오**: `aux.c`, `con.h`, `prn.txt` 와 같은 예약 파일명을 포함하는 파일로그 저장소를 가상으로 빌드합니다.
* **단언 및 검증 기준**: `NodeIdUtil.encodeFname`을 통해 물리 스토어상에서 안전한 티에라(`~xx`) 이스케이프 형식으로 정상 변환되는지 단언합니다.

### 🗺️ 13. 비ASCII 다국어(UTF-8 한글 등) 파일명 디스크 세이프 fncache 인코딩 및 복원 테스트
* **💡 네이티브 특성**: 다국어 파일명이 물리 스토어상에 보존될 때, 플랫폼 인코딩 충돌을 방지하기 위해 안전하게 이스케이프되어 fncache에 매핑 기입됩니다.
* **🧪 시나리오**: '한글명저장소/테스트.txt' 와 같은 비ASCII 유니코드 경로에 대해 인코딩을 수행합니다.
* **단언 및 검증 기준**: 스토어 디바이스의 다국어 경로가 데이터 접두사 `data/`를 품고 안전하게 세이프 이진 바이트로 직렬화되는지 단언합니다.

### 🗺️ 14. 255바이트 초과 초장기 디렉토리 경로에 대한 `dh/` 패턴 하이브리드 해싱 검증 테스트
* **💡 네이티브 특성**: 물리 디스크 경로가 255바이트 한계를 초과하면, 파일 시스템 제약을 극복하기 위해 `dh/` (Directory Hash) 디렉토리 하위에 SHA-1 해시로 파일명을 압축 보존합니다.
* **🧪 시나리오**: 깊이가 수십 겹인 초장기 가상 경로(약 300바이트)에 대한 해싱 기입 연산을 수행합니다.
* **단언 및 검증 기준**: 해싱된 결과 경로가 `dh/` 접두사와 함께 유일무이한 해시 문자열을 성공적으로 생성해 내는지 단언합니다.

### 🗺️ 15. `fncache` 인코딩 도중 중복 접두사 기입 방지 및 해시 중복 매핑 방어 테스트
* **💡 네이티브 특성**: 긴 파일명 인코딩 중 `meta/` 또는 `data/` 디렉토리 접두사가 중복 부착되어 해시가 잘못 생성되거나 중복 생성되는 버그를 원천 예방해야 합니다.
* **🧪 시나리오**: `meta/data/long_file_name...` 형태의 모킹된 경로를 인코딩 호출합니다.
* **단언 및 검증 기준**: 인코더가 경로의 이중 처리를 감지하고 1개의 정형화된 접두사만 안전하게 남겨두는지 검증합니다.

### 🗺️ 16. `fncache` 디스크 유실 시 전체 스토어 파일 스캐닝을 통한 자동 Rebuild 복구 테스트
* **💡 네이티브 특성**: `.hg/store/fncache` 파일이 삭제되거나 손상되는 재해가 닥쳤을 때, `debugrebuildfncache` 엔진은 물리 스토어 폴더들을 전수 스캔하여 역으로 fncache 명단을 복원해 냅니다.
* **🧪 시나리오**: 수십 개의 리비전이 있는 가상 스토어에서 fncache 파일을 지우고 스토어 스캐너 복원 루프를 동작시킵니다.
* **단언 및 검증 기준**: 스캔 완료 후 새로 생성된 fncache 데이터의 경로 목록이 유실 전 데이터와 한 글자도 다름없이 100% 복구되는지 단언합니다.

---

## 📂 카테고리 4: 동시성, 락 및 트랜잭션 영역 (Concurrency & Transactions)

### 🗺️ 17. 다중 프로세스 간의 파일 시스템 OS 수준 `FileLock` 획득 대기 및 타임아웃 획득 실패 시 복구 테스트
* **💡 네이티브 특성**: 여러 프로세스(예: native hg + hg4j)가 동일 저장소에 동시 진입하려 할 때, 파일 시스템 수준의 exclusive `FileLock`을 안전하게 처리하고 획득 실패 시 적절히 차단 대기해야 합니다.
* **🧪 시나리오**: 별도 프로세스 락 소유 상황을 시뮬레이션하고 hg4j 측에서 쓰기 트랜잭션을 진입시킵니다.
* **단언 및 검증 기준**: 락 획득 실패 시 무한 대기에 빠지지 않고 즉시 안전한 락 타임아웃 예외를 뿜어내는지 단언합니다.

### 🗺️ 18. 동일 JVM 내부 다중 스레드 경쟁 상황 하에서 `SafeFileIO`와 `Lock`의 상호 배제 무결성 테스트
* **💡 네이티브 특성**: 동일 JVM 내에서 다중 스레드가 동일한 자바 `FileChannel.lock()`을 호출하면 `OverlappingFileLockException`이 발생하며 붕괴하므로, 내부 스레드 간에는 `synchronized` 혹은 `ReentrantLock`으로 사전 동기화 교통정리를 해야 합니다.
* **🧪 시나리오**: 10개 이상의 병렬 스레드가 동일한 타겟 파일에 대해 `SafeFileIO.writeAtomic`을 일제히 격발합니다.
* **단언 및 검증 기준**: 다중 스레드 하에서도 겹침 예외(Overlapping Exception) 없이 완벽하게 순차 원자적 쓰기가 완수되는지 검증합니다.

### 🗺️ 19. 락 파일(`wlock`, `lock`)의 Stale Lock(좀비 락) 잔존 시 경고 감지 및 수동 강제 해제 안전성 테스트
* **💡 네이티브 특성**: 비정상 강제 종료 등으로 인해 `.hg/wlock` 파일이 미처 지워지지 못하고 남은 경우(Stale lock), 일정 시간이 흐르거나 사용자의 명시적 요청 시 이를 감지하고 강제 락 클리어(lock cleanup)를 수행합니다.
* **🧪 시나리오**: 임의의 오래된 stale wlock 파일(예: 1시간 전)을 생성하고 쓰기를 시도합니다.
* **단언 및 검증 기준**: 락 수동 정리 API를 통해 안전하게 잔존 락이 해제되며 다음 쓰기가 정상으로 수행되는지 단언합니다.

### 🗺️ 20. 단독 기입 트랜잭션 진행 중 캐시 무효화 바이패스(Cache Bypass) 무결성 테스트
* **💡 네이티브 특성**: 로컬 쓰기 트랜잭션이 한창 진행되어 신규 리코드가 `addedRecords` 메모리 버퍼에 쌓이고 있을 때, 디스크 캐시가 중도에 초기화되거나 무효화(invalidate)되면 정합성이 깨집니다.
* **🧪 시나리오**: 신규 쓰기가 한창 진행되는 버퍼 상태에서 외부 캐시 체크를 호출합니다.
* **단언 및 검증 기준**: 트랜잭션 진행 중에는 캐시 체크 및 초기화가 바이패스되어 메모리상의 쓰기 트랜잭션 무결성이 유지되는지 단언합니다.

### 🗺️ 21. 트랜잭션 장애 주입 저널 롤백 무결성 테스트 (Crash Injection & Journal Rollback)
* **💡 네이티브 특성**: 커밋/쓰기 작업 진행 중 예기치 않은 디스크 풀(Disk Full)이나 예외가 닥칠 경우, 기록 중이던 트랜잭션 저널(`.hg/store/journal` 및 `.hg/undo`)을 읽어서 이전 상태로 깔끔하게 롤백 복구해야 합니다.
* **🧪 시나리오**: 커밋 데이터를 기입하는 중간 과정에서 디스크 입출력 예외를 강제 주입(Fault Injection)하고 롤백 엔진을 구동합니다.
* **단언 및 검증 기준**: 원복 처리 완료 후 스토어 내 모든 리비전 파일 크기와 오프셋이 장애 발생 전의 깨끗한 상태로 100% 원복됨을 보증합니다.

---

## 📂 카테고리 5: 네트워크, 프로토콜 및 와이어 포맷 영역 (Network & Wire Formats)

### 🗺️ 22. SSH Stdio V2 프로토콜 규격 승격 시 5바이트 프레임 헤더 스트림 파이핑 검증 테스트
* **💡 네이티브 특성**: SSH Stdio V2 협상 성공 시, 모든 통신은 `Channel ID (1Byte) + Payload Length (4Byte)` 규격의 5바이트 프레임 헤더로 철저하게 감싸져 송수신되어야 합니다.
* **🧪 시나리오**: V2 프로토콜 모킹 클라이언트를 가동해 raw 커맨드가 5바이트 래퍼를 정상 통과하는지 확인합니다.
* **단언 및 검증 기준**: 출력 바이너리 스트림의 헤더 시그니처와 채널 정보가 완벽하게 V2 프레임 규격으로 패킹되는지 단언합니다.

### 🗺️ 23. CBOR 기반 HTTP Wire Protocol V2 API 감지 및 와이어 협상 실패 시 V1 프로토콜 폴백 정합성 테스트
* **💡 네이티브 특성**: 원격 머큐리얼 서버가 대용량 고속 전송을 위해 HTTP V2 프로토콜(`application/mercurial-x-api-v2`)을 지원할 때, 클라이언트가 이를 감지하여 연동하되 지원하지 못할 시 V1 표준(`mercurial-0.1`)으로 정상 폴백 조율해야 합니다.
* **🧪 시나리오**: V2만을 요구하는 환경 및 V1/V2 복합 환경을 모킹하여 원격 연결을 수립합니다.
* **단언 및 검증 기준**: CBOR 프레임 감지 경계 조건에서 안전하게 V1 폴백이 성사되고 호환 연결이 유지되는지 검증합니다.

### 🗺️ 24. SSH stdio 스트림 에러 프레임(Error Frame) 감지 및 원격 네트워크 연결 예외 전파 테스트
* **💡 네이티브 특성**: SSH 통신 도중 원격 서버 프로세스에서 에러 채널(Channel 2) 혹은 시스템 오류 프레임이 송출되었을 때, 세션 먹통 상태가 되지 않고 즉시 클라이언트 수준의 구조화된 예외로 즉각 상위 전파되어야 합니다.
* **🧪 시나리오**: 원격 에러 스트림에 훼손된 에러 패킷 프레임을 주입합니다.
* **단언 및 검증 기준**: 무한 대기 상태에 빠지지 않고 예외가 상위 전파되어 접속이 안전하게 종료되는지 단언합니다.

### 🗺️ 25. Bundle2 Multipart 스트림 수신 시 'HG20' 매직 시그니처 판독 및 파트별 프레임 분할 파싱 테스트
* **💡 네이티브 특성**: Mercurial의 대량 패킷 전송용 와이어 데이터인 `Bundle2` 포맷은 반드시 파일 선두에 `HG20` 매직 문자열을 수반하며, 이후 멀티파트 청크 스트림 구조로 이어집니다.
* **🧪 시나리오**: 가상의 `HG20\0\0` 기반 번들2 패킷 바이너리 데이터 스트림을 준비하고 파서에 먹입니다.
* **단언 및 검증 기준**: 매직 시그니처 판독과 함께 개별 파트별 데이터가 정상적으로 분할 디코딩되는지 단언합니다.

### 🗺️ 26. Phase 전이 모델(Public ↔ Draft ↔ Secret)에 따른 커밋 배포 격리 및 비밀 커밋 Push 원천 차단 테스트
* **💡 네이티브 특성**: Mercurial은 커밋별 상태를 `Public`(불변/공유), `Draft`(로컬/수정가능/공유가능), `Secret`(공유원천불가)으로 격리 관리하며, `Secret`으로 표시된 리비전은 원격 `push/pull` 시 절대 전송되지 않고 은폐되어야 합니다.
* **🧪 시나리오**: Draft 커밋이 Push를 타는 순간 Public으로 전이되는 것과, Secret 커밋을 포함하여 Push를 시도하는 상황을 시뮬레이션합니다.
* **단언 및 검증 기준**: Secret 커밋은 원격 푸시 명단에서 철저히 제외 및 격리되며, 일반 드래프트 커밋은 전송 성공 시 Public으로 상태 전이됨을 보증합니다.

---

## 📂 카테고리 6: 초대규모 성능 벤치마크 검증 영역 (Performance Benchmark)

### 🗺️ 27. [Perf-1] 수백만 리비전 힙 메모리 OOM 스트레스 테스트 (Revision Lazy-loading Test)
* **💡 네이티브 특성**: 대규모 모노레포에서는 리비전 변경셋 수가 100만 개를 훌륭히 초과합니다. 힙 메모리를 초저용량(64MB)으로 제한해도 힙 고갈 OOM 없이 그래프를 신속 탐색하기 위해 `SoftReference` 기반 LRU 캐시와 Lazy-loading 오프셋 seek 메커니즘을 결합해야 합니다.
* **🧪 시나리오**: 50만 개의 가상 리비전 인덱스를 적재한 채 무작위 Lazy load 및 Soft-ref 적재 연산을 10,000회 고속 격발합니다.
* **단언 및 검증 기준**: 가용 메모리 압박 상태에서도 탐색 연산이 평균 `10ms` 이하로 완수되며, OOM 없이 300ms 내에 1만 회의 무작위 탐색이 성사되는지 단언합니다.

### 🗺️ 28. [Perf-2] LCS 및 Myers Diff 다중 스레드 동시성 스트레스 테스트 (Myers Diff Stress Test)
* **💡 네이티브 특성**: 대규모 커밋 혹은 병렬 빌드가 다중 스레드상에서 진행될 때, Myers Diff 알고리즘 연산 엔진이 락 없이 완벽한 스레드 안전성을 발휘하며 초고속으로 LCS 변경분을 산출해야 합니다.
* **🧪 시나리오**: 8대 병렬 스레드를 가동하여 각각 1,000라인짜리 대형 모킹 텍스트의 Myers Diff 연산을 총 200회 중복 격발합니다.
* **단언 및 검증 기준**: 다중 스레드가 격렬히 부딪혀도 스레드 안전성이 확보되며, 전체 병렬 연산이 2초 이내로 성공리에 마무리되는지 단언합니다.

### 🗺️ 29. [Perf-3] 하이브리드 wlock/lock 고밀도 동시성 경합 테스트 (Locking Congestion Test)
* **💡 네이티브 특성**: JVM 다중 스레드 경쟁이나 외부 OS 프로세스 간 저장소 동시 수정 요청이 폭주하는 극한 상황에서도 `FileLock` 겹침 예외(OverlappingException)를 회피하고 정합성 있는 원자적 디스크 기입을 성사시켜야 합니다.
* **🧪 시나리오**: 10개의 병렬 스레드가 하이브리드 락킹(일부는 wlock bypass, 일부는 OS lock fallback)을 거쳐 동일 파일에 원자적으로 일제히 쓰기를 요청합니다.
* **단언 및 검증 기준**: 겹침 예외나 데드락 없이 안정적으로 순차 락을 획득하여 1.5초 이내에 성사되는지 단언합니다.

### 🗺️ 30. [Perf-4] 인라인(Inline) -> 아웃라인(Outline) 대용량 스위칭 벤치마크 (Store Conversion Test)
* **💡 네이티브 특성**: 단일 파일 인라인 구조로 커밋하다가 파일 크기가 10MB 임계치를 순간 돌파할 때, 디바이스의 데이터 쓰기 오버헤드를 줄이기 위해 별도 `.d` 파일을 분리해 내는 스위칭 연산 지연 속도가 실시간 성능 한계 내에 완료되어야 합니다.
* **🧪 시나리오**: 10MB 크기 임계치 돌파를 모킹하여 비인라인 아웃라인 데이터 파일 분할 감지 속도를 측정합니다.
* **단언 및 검증 기준**: 스토어 감지 및 비인라인 스위칭 전환 모니터링 연산 속도가 단 50ms 미만으로 신속하게 완수되는지 단언합니다.

---

**Antigravity pair-programming team.**
